export interface Property {
  id: string;
  name: string;
  image: string;
  address: string;
  description?: string;
  overview?: any;
}

export const properties: Property[] = [
  // copy the existing objects from PropertyList.tsx or PropertyDetailPage.tsx
];
